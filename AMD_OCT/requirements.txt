torch>=2.0
torchvision>=0.15
numpy>=1.23
pandas>=1.5
pillow>=9.0
scikit-learn>=1.2
scipy>=1.9
tqdm>=4.64
hydra-core>=1.3
omegaconf>=2.3
kagglehub>=0.2

# Optional backbone dependencies. Install the ones you need:
#   pip install -r requirements.txt                 # core only (EfficientNet, ConvNeXt)
#   pip install -e .[biomedclip]                    # + BiomedCLIP backbone
#   pip install -e .[mirage]                        # + MIRAGE ViT-Base backbone
#   pip install -e .[medvit]                        # + MedViT backbone (natten)
#   pip install -e .[all]                           # everything except natten/medvit
open_clip_torch>=2.24
safetensors>=0.4
huggingface_hub>=0.20
